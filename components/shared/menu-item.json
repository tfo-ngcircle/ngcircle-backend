{
  "collectionName": "components_sections_menu_items",
  "info": {
    "name": "Link",
    "icon": "list",
    "description": "All Item names must begin with a capitial letter and destinations with a foward slash"
  },
  "options": {},
  "attributes": {
    "label": {
      "type": "string",
      "required": true,
      "regex": "\\b[A-Z].*?\\b",
      "unique": false,
      "maxLength": 32
    },
    "destination": {
      "type": "string",
      "unique": false,
      "default": "/",
      "regex": "^\\/.+",
      "required": true
    }
  }
}
